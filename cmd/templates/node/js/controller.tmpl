class {{NAME}}Controller {
    async index(req, res) {
        try {
            return res.status(200).json({ message: "Success from {{NAME}}" });
        } catch (error) {
            return res.status(500).json({ error: error.message });
        }
    }
}

module.exports = new {{NAME}}Controller();